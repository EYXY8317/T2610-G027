// 设置项定义（元数据），不是逻辑代码——见
// settings/appearance/borderRadiusSetting.js 里的说明。
// min/max 限定了这个数值设置允许的取值范围。
// A setting definition (metadata), not logic code — see
// settings/appearance/borderRadiusSetting.js for the full explanation.
// min/max bound the allowed range for this numeric setting.
export const flipClockSizeSetting = {

    id: "flip-clock-size",

    label: "Flip Clock Size",

    defaultValue: 80,

    min: 40,

    max: 160

};
