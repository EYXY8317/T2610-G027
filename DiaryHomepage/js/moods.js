// 转发文件——重新导出 mood_sync.js 里的真正实现（心情列表数据源）。
// Re-export file — forwards the real implementation from mood_sync.js
// (the shared source of truth for the mood list).
export { MOOD_LIST } from './mood_sync.js';
